// Generated by CoffeeScript 1.12.2
(function() {
  window.timeStampToDateStr = function(stamp) {
    var date, day, month, year;
    date = new Date(stamp);
    year = date.getFullYear();
    month = date.getMonth();
    day = date.getDate();
    return month + '/' + day + " " + year;
  };

  window.paramsToRequestOptions = function(params) {
    var key, urlSearchParams;
    urlSearchParams = new URLSearchParams();
    for (key in params) {
      urlSearchParams.set(key, params[key]);
    }
    return urlSearchParams;
  };

  window.relUrlToBackAbsUrl = function(url) {
    return XzrtConfig.BACKWARD_ROOT_URL + url;
  };

  window.urlToQuery = function(url) {
    var search;
    search = url.split('?')[1];
    return JSON.parse('{"' + decodeURI(search).replace(/"/g, '\\"').replace(/&/g, '","').replace(/=/g, '":"') + '"}');
  };

}).call(this);

//# sourceMappingURL=converter.js.map
